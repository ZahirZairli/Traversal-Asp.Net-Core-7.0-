@model EntityLayer.Concrete.Reservation
@{
    ViewData["Title"] = "NewReservation";
    Layout = "~/Views/Shared/_UserLayout.cshtml";
    ViewData["page"] = "newres";
}

<div class="row">
    <div class="col-12">
        <div class="card mb-4">
            <div class="card-header pb-0">
                <h6>Rezervasiya</h6>
                <hr />
            </div>
            <div class="card-body px-0 pt-0 pb-2">
                <div class="table-responsive p-0" style="padding: 40px !important;">
                    <form class="form text-left" method="post">
                        <div class="mb-3">
                            <label>Adam sayı</label>
                            <input required type="number" min="0" class="form-control" asp-for="PersonCount">
                            <span class="text-danger" asp-validation-for="PersonCount"></span>
                        </div>
                        <div class="mb-3">
                            <label>Tarix</label>
                            <input required type="datetime-local" class="form-control" asp-for="Date">
                            <span class="text-danger" asp-validation-for="Date"></span>
                        </div>
                        <div class="mb-3">
                            <label>Ətraflı</label>
                            <textarea required asp-for="Description" class="form-control"></textarea>
                            <span class="text-danger" asp-validation-for="Description"></span>
                        </div>
                        <div class="mb-3">
                            <label>Tur</label>
                            @Html.DropDownListFor(x => x.DestinationId, (List<SelectListItem>)ViewBag.destination, new { @class = "form-control" })
                            <span class="text-danger" asp-validation-for="Destination"></span>
                        </div>
                        <div class="text-danger" asp-validation-summary="ModelOnly"></div>
                        <div class="text-center">
                            <button type="submit" class="btn bg-gradient-primary w-100 my-4 mb-2">Rezervasİya et</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

